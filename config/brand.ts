/**
 * Brand identity. `name` is used for metadata / aria labels; the logo
 * renders a two-tone wordmark from wordA + wordB.
 */
export const BRAND = {
   name: 'Nextblogs',
   wordA: 'Next',
   wordB: 'blogs',
   tagline: 'Words in black & white.',
}

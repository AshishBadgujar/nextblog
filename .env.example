# Local development needs NONE of these — it uses storage/nextblogs.db automatically.

# Production (Vercel): hosted SQLite via Turso
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your-turso-token
